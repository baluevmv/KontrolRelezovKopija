
#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Список, 
		"СтатусОбработки", 
		ОбщегоНазначенияКлиентСервер.ПредопределенныйЭлемент("Перечисление.СтатусыОбработкиЗапросовНаАкцепт.Новый"),
		ВидСравненияКомпоновкиДанных.Равно,,
		Истина,
		РежимОтображенияЭлементаНастройкиКомпоновкиДанных.Обычный);
	
	НастроитьЭлементыФормы();
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Принять(Команда)
	
	ТекущиеДанные = Элементы.Список.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ЗапросыНаАкцептИБ.ПринятьЗапросНаАкцепт(ТекущиеДанные.Ссылка);
	
	Элементы.Список.Обновить();
	Оповестить("ОбработкаЗапросаАкцептИБ");
	
КонецПроцедуры

&НаКлиенте
Процедура Отклонить(Команда)
	
	ТекущиеДанные = Элементы.Список.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ЗапросыНаАкцептИБ.ОтклонитьЗапросНаАкцепт(ТекущиеДанные.Ссылка);
	
	Элементы.Список.Обновить();
	Оповестить("ОбработкаЗапросаАкцептИБ");
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСкрытьJSON(Команда)

	ПоказыватьJSON = Не ПоказыватьJSON;
	
	НастроитьЭлементыФормы();

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	
	ТекущиеДанные = Элементы.Список.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		ТелоЗапроса = "";
		Возврат;
	КонецЕсли; 
	
	ТелоЗапроса = ПолучитьТелоЗапроса(ТекущиеДанные.Ссылка);
	
	НастроитьЭлементыФормы();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура НастроитьЭлементыФормы()
	
	ТекущиеДанные = Элементы.Список.ТекущиеДанные;
	ДоступностьКоманд = ?(ТекущиеДанные = Неопределено, Ложь, Истина);
	
	Элементы.СписокПринять.Доступность = ДоступностьКоманд;
	Элементы.СписокОтклонить.Доступность = ДоступностьКоманд;
	
	Элементы.СписокПоказатьСкрытьJSON.Заголовок = ?(ПоказыватьJSON, "Скрыть JSON", "Показать JSON");
	Элементы.СписокПоказатьСкрытьJSON.Пометка = ПоказыватьJSON;
	
	Элементы.ГруппаТелоЗапроса.Видимость = ПоказыватьJSON;
	Элементы.ТелоЗапроса.ТолькоПросмотр = Истина;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьТелоЗапроса(ЗапросНаАкцептИБ)
	
	ТелоЗапроса = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЗапросНаАкцептИБ, "ТелоЗапроса");
	Возврат РаботаСJSON.ФорматироватьСтроку(ТелоЗапроса.Получить(), ПереносСтрокJSON.Авто);
	
КонецФункции

#КонецОбласти